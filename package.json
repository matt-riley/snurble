{
  "name": "@matt-riley/snurble",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --dir apps/docs dev",
    "lint": "pnpm exec oxlint -c oxlint.lint.config.ts . && pnpm exec oxfmt --check .",
    "typecheck": "pnpm exec tsc --noEmit -p packages/ui-astro/tsconfig.json && pnpm --dir apps/docs typecheck",
    "test": "pnpm exec vitest run",
    "build": "pnpm --dir apps/docs build",
    "validate": "pnpm run check && pnpm run typecheck && pnpm run test && pnpm run build",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "lefthook": "^2.1.5",
    "oxfmt": "^0.46.0",
    "oxlint": "^1.59.0",
    "typescript": "5.9.3",
    "ultracite": "7.5.6",
    "vitest": "^4.1.3"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.33.0"
}
