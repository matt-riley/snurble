---
---

<dl class="snurble-meta-list">
  <slot />
</dl>

<style>
  .snurble-meta-list {
    display: grid;
    grid-template-columns: minmax(0, 10rem) minmax(0, 1fr);
    gap: var(--snurble-space-2) var(--snurble-space-4);
    margin: 0;
    min-width: 0;
  }

  .snurble-meta-list :global(dt) {
    grid-column: 1;
    margin: 0;
    color: var(--snurble-text-muted);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .snurble-meta-list :global(dd) {
    grid-column: 2;
    margin: 0;
    min-width: 0;
    color: var(--snurble-text);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  .snurble-meta-list :global(dd p) {
    margin: 0;
  }

  .snurble-meta-list :global(dd p + p) {
    margin-block-start: var(--snurble-space-1);
  }

  .snurble-meta-list :global(a) {
    color: var(--snurble-accent);
  }

  .snurble-meta-list :global(code) {
    white-space: nowrap;
  }

  @media (max-width: 40rem) {
    .snurble-meta-list {
      grid-template-columns: 1fr;
      gap: var(--snurble-space-1);
    }

    .snurble-meta-list :global(dt),
    .snurble-meta-list :global(dd) {
      grid-column: 1;
    }

    .snurble-meta-list :global(dd) {
      margin-block-end: var(--snurble-space-2);
    }
  }
</style>
