---
---

<div class="surble-panel">
  <slot />
</div>

<style>
  .surble-panel {
    min-width: 0;
    padding: var(--surble-space-5);
    color: var(--surble-text);
    background: var(--surble-surface);
    border: 1px solid var(--surble-border);
    border-radius: 1.5rem;
    box-shadow: 0 1.5rem 3rem rgb(0 0 0 / 0.16);
  }
</style>
